# Direct dependencies only - pip resolves transitive deps.
# Versions are known-good floors from the original working freeze.
fastapi>=0.141,<1
uvicorn[standard]>=0.52,<1          # standard: watchfiles (reload), websockets, httptools
jinja2>=3.1,<4
python-dotenv>=1.0,<2
python-multipart>=0.0.32,<0.1       # file uploads
httpx>=0.28,<1                      # Telegram notifications + TestClient
kaggle>=2.2.4,<3                    # Kaggle CLI subprocess backend
